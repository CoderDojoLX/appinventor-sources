language: java
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libc6-i386 lib32stdc++6 lib32gcc1 lib32ncurses5 lib32z1
install: ant deps
script:
  - ant MakeAuthKey 
  - ant tests
